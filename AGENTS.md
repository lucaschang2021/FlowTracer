# FlowTracer 协作规则

本仓库按阶段闸门推进：架构冻结 → 后端 → 前端 → 集成 → 发布。每个 Phase 必须在新的 Codex 任务中执行；开始时只读取 `docs/CURRENT-GATE.md`、本阶段准入/基线文件、相关接口契约和本次改动文件，不回读整套历史文档。若 `CURRENT-GATE` 与仓库提交、看板或 PR 事实不一致，先停止并报告总控，不得自行推断准入。

质量基线不得因节省额度而降低：业务实现、架构决策、数据库迁移、跨模块集成和正式 Code Review 默认使用 GPT-5.6 Sol / medium；安全、并发、数据一致性、复杂迁移或重大故障可提升到 high。Terra/Luna 仅用于 Git 状态、格式化、日志归纳、文档机械整理等非决策任务，不得独立批准阶段门禁或高风险变更。

`main` 是稳定分支。开发在已授权的 `feat/*` 或 `fix/*` 分支完成 commit、push、PR、Review、Merge；不得直接推送或合并 `main`。外部网络、push、PR 创建/评论、merge、发布及任何会改变共享基础设施的动作仍须明确授权。不得修改其他 worktree；总控不编写大型业务代码。

优先批量运行相互独立的只读检查。先在沙箱执行；仅当实际因权限、网络或沙箱限制失败时，才用最小范围申请提权。成功命令只记录结论、版本和关键计数，不粘贴冗长日志。未变更的同一 commit 不重复运行全量测试；开发者每个 commit 最多一次全量测试，总控只在最终待合并提交上独立复跑一次。任何失败、迁移、公开 API、评分或 Pipeline 变化必须报告并遵循 ADR/契约变更控制。P0/P1 未清零，禁止批准下游阶段。

开发角色完成一个 Phase 后必须停点，提交变更范围、commit、迁移、测试、风险与 PR 报告，等待总控书面许可；未经许可不得进入下一 Phase。总控验收应核对范围、提交、架构、测试、安全、可重复启动和跨模块影响。
